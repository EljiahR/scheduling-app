import Card from "../Card"
import styles from "./AuthPage.module.css"

export default () => {
    return (
        <div id={styles.authpage}>
            <Card styles="width: 80%; height: 400px;">Auth</Card>
        </div>
    )
}