import "../../global.css";
import styles from "./NavBar.module.css";

export default () => {
    return (
        <nav id={styles.navbar} class="theme">
            <div>Hello World</div>
        </nav>
    )
}